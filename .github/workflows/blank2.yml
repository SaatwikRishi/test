name: Trigger Workflow

on:
  push:
    branches:
      - main

jobs:
  trigger:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Trigger workflow in repo-B
        run: |
          curl -X POST \
          -H "Accept: application/vnd.github.v3+json" \
          -H "Authorization: Bearer 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" \
          "https://api.github.com/repos/SaatwikRishi/test/actions/workflows/blank/dispatches" \
          -d '{"ref": "main", "input": {"files": "a.json,b.json"}}'

